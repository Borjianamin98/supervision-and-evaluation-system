import Typography from "@material-ui/core/Typography";
import React from 'react';

const ProblemView: React.FunctionComponent = () => {
    return (
        <Typography dir="rtl" paragraph>
            مسئله‌ها
        </Typography>
    );
}

export default ProblemView;